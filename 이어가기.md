# 이어가기

내일 PC에서 이 파일부터 보시면 됩니다.

마지막 저장 : 2026-09-20
브랜치 : `claude/claude-app-planning-5co04d`
테스트 : 327개 전부 통과

---

## 1. 내일 맨 처음 할 것

터미널을 한 번만 열면 됩니다. 그다음부터는 안 열어도 됩니다.

```bash
git clone https://github.com/sky98110027-cmyk/-2.git 결
cd 결
git checkout claude/claude-app-planning-5co04d
```

그리고 폴더에서 **`start.command`** (맥) 또는 **`start.bat`** (윈도우)를 두 번 누릅니다.

처음 켤 때 클로드 API 키를 한 번 물어봅니다.
`.env` 파일은 깃에 안 올라가니 PC에서 다시 한 번 넣으셔야 합니다.
[console.anthropic.com](https://console.anthropic.com) 에서 받으시면 됩니다.

### ffmpeg 도 깔아주세요

이게 있어야 **프레임 뜯기**와 **조립**이 됩니다.

```bash
brew install ffmpeg      # 맥
```

윈도우는 [ffmpeg.org](https://ffmpeg.org) 에서 받아 압축 풀고 PATH에 넣습니다.

---

## 2. 지금까지 만든 것

### 되는 것

| 무엇 | 어디서 |
| --- | --- |
| 말 한 줄로 전부 시키기 | **말로 하기** 탭 |
| 영상 주소에서 결 뽑기 | 주소 붙여넣기 |
| 받아둔 파일에서 결 뽑기 | `.txt` `.srt` `.vtt` `.ass`, 자막 박힌 영상 |
| 결을 클로드 스킬로 저장 | `.claude/skills/` 에 같이 깔림 |
| 대본 + 장면별 영문 프롬프트 | **대본 만들기** 탭 |
| 여러 편 한 번에 (동시에 돌아감) | `홍범도, 김좌진, 안중근 세 개 써줘` |
| 폴더 이름 직접 짓기 (한글 됨) | 말로도, 칸에도 |
| 자막 글꼴·색·위치·크기 고치기 | **고치기** 탭, 미리보기 즉시 반영 |
| 말로 고치기 | `자막 크게 노란색으로` |
| 레퍼런스 프레임 단위로 뜯기 | **내 결** 탭 → 화면까지 뜯기 |
| 타임라인 · 레이어 여섯 | **영상 만들기** 탭 |
| 일레븐랩스로 나레이션 읽기 | 실제로 붙음 |
| ffmpeg 로 mp4 묶기 | **아직 실제 시험 안 함** ← 내일 첫 할 일 |
| API 키 · MCP 연결 | **연결** 탭 |

### 레이어 여섯

```
화면    장면 그림/영상   ← 힉스필드 · 톱뷰 · 내 그림 폴더
캐릭터  밈, 스티커       ← 내 캐릭터 폴더 (PNG)
자막    화면 위 글씨     ← 이 앱 (ASS)
목소리  나레이션         ← 일레븐랩스 (붙음)
환경음  바람, 발소리     ← (꽂을 자리, 비어 있음)
배경음  음악             ← 파일 또는 (꽂을 자리)
```

---

## 3. 내일 할 일 (순서대로)

### 1순위 — ffmpeg 조립을 진짜로 돌려보기

**이게 제일 급합니다.** 지금까지 명령이 제대로 만들어지는지까지만 봤습니다.
실제로 mp4가 나오는 걸 아직 아무도 못 봤습니다.

이렇게 시험하시면 됩니다.

1. 대본을 하나 만듭니다 (`홍범도로 1분짜리 써줘`)
2. **영상 만들기** 탭에서 작업 폴더 경로를 확인합니다
3. 그 안 `screen/` 폴더에 아무 그림이나 `01.png` `02.png` … 로 넣습니다
   (장면 수만큼. 진짜 그림이 아니어도 됩니다. 일단 나오는지만 보면 됩니다)
4. **조립해서 mp4로 뽑기** 를 누릅니다

안 되면 그 오류 메시지를 그대로 저한테 주시면 됩니다.

### 2순위 — 유튜브 업로드를 화면에 잇기

`src/gyeol/youtube.py` 는 다 만들어져 있습니다. 로그인, 채널 확인, 올리기 전부요.
**통로(API)와 화면이 아직 없습니다.** 그것만 붙이면 됩니다.

안전장치는 이미 코드에 박혀 있습니다.
- 기본 공개 범위는 항상 **비공개**
- 확인 버튼 안 누르면 파일도 안 열어봄

붙이기 전에 구글 클라우드에서 열쇠 파일을 받으셔야 합니다. 10분쯤 걸립니다.

### 3순위 — 화면 레이어를 실제로 잇기

지금 화면 레이어는 **프롬프트만** 뽑습니다.
힉스필드나 톱뷰 MCP를 붙이면 그림까지 자동으로 뽑게 만들 수 있습니다.

MCP 주소는 그 회사 설정 페이지에서 받아 **연결** 탭에 넣으시면 됩니다.
제가 주소를 지어내면 안 돼서 비워뒀습니다.

### 4순위 — 환경음·음악 자리 채우기

지금 `(꽂을 자리)` 로 비어 있습니다. 쓸 API를 정하시면 붙이겠습니다.

---

## 4. 알아두실 함정

- **`.env` 는 깃에 안 올라갑니다.** PC에서 키를 다시 넣으셔야 합니다. 일부러 그렇게 했습니다.
- **`data/` 폴더도 안 올라갑니다.** 지금까지 만든 결과물은 이 컨테이너에만 있습니다.
  내일은 빈 상태에서 시작합니다.
- **ffmpeg 없으면** 프레임 뜯기와 조립이 안 됩니다. 나머지는 다 됩니다.
- **모든 프레임을 보는 게 아닙니다.** 장면 바뀌는 지점마다 한 장입니다.
- **환경음이 무슨 소리인지는 못 듣습니다.** 말이 없는 구간이 침묵인지 음악만인지만 잽니다.

---

## 5. 파일 어디에 뭐가 있나

```
src/gyeol/
  router.py      말 한 줄을 어느 일로 보낼지 가른다
  guide.py       앱 사용법을 클로드가 직접 답한다
  source.py      유튜브 자막 받기 (시간 붙은 자막 포함)
  intake.py      받아둔 파일 받기 (.srt .txt 영상)
  analyze.py     자막에서 스타일 DNA 뽑기
  deep.py        프레임 단위로 뜯기 (ffmpeg + 클로드 눈)
  skillgen.py    스타일 DNA를 SKILL.md 로 굽기
  produce.py     결 + 주제 → 대본, 레퍼런스 모양 따라가기
  timeline.py    타임라인과 레이어 여섯
  providers.py   일레븐랩스, 폴더 제공자
  assemble.py    ffmpeg 로 mp4 묶기
  revise.py      말로 고치기
  style.py       자막 꾸밈새 (글꼴 색 위치)
  connect.py     API 키, MCP, 준비 점검
  youtube.py     유튜브 올리기 (화면에 아직 안 이음)
  store.py       파일로 보관
  cli.py         터미널에서 쓰기

src/static/      화면 (index.html, app.js, styles.css)
tests/           327개
start.py         시작 파일이 부르는 것
```

---

## 6. 터미널이 편하시면

```bash
cd src
python -m gyeol 결뽑기 "https://youtu.be/..." --저장
python -m gyeol 결뽑기 --파일 받아둔영상.mp4 --저장
python -m gyeol 목록
python -m gyeol 대본 <결이름> "김좌진과 청산리" --분 8 --폴더 "독립군 3부작"
python -m gyeol 서버
```

---

## 7. 저한테 이어서 물으실 때

이 한 줄이면 제가 바로 따라잡습니다.

> 결 앱 이어서 하자. `이어가기.md` 읽고 시작해줘.
