# 윈도우에서 시작하기

사무실 PC(윈도우)에서 처음 여실 때 이 순서대로만 하시면 됩니다.

세 번만 깔면 됩니다. **파이썬, 이 프로그램, ffmpeg.**

---

## 1단계 — 파이썬 깔기 (5분)

1. [python.org/downloads](https://www.python.org/downloads/) 에 들어갑니다
2. 노란 **Download Python** 단추를 누릅니다
3. 받은 파일을 두 번 누릅니다
4. **⚠️ 맨 아래 `Add python.exe to PATH` 에 반드시 체크하세요**
   이걸 빼먹으면 나중에 아무것도 안 됩니다
5. **Install Now** 를 누릅니다

제대로 깔렸는지 보려면, 시작 버튼 옆 검색창에 `cmd` 치고 엔터 친 다음 이걸 칩니다.

```
python --version
```

`Python 3.13.x` 같은 게 나오면 된 겁니다.

---

## 2단계 — 이 프로그램 받기 (2분)

### 쉬운 방법 (추천) — 압축 파일로

1. 이 주소를 엽니다
   `https://github.com/sky98110027-cmyk/-2/archive/refs/heads/claude/claude-app-planning-5co04d.zip`
2. 받아진 압축 파일을 **바탕화면**에 풉니다
3. 풀린 폴더 이름이 길면 **`결`** 로 바꿔주세요

### 깃을 쓰실 줄 알면

```
cd %USERPROFILE%\Desktop
git clone https://github.com/sky98110027-cmyk/-2.git 결
cd 결
git checkout claude/claude-app-planning-5co04d
```

---

## 3단계 — ffmpeg 깔기 (3분)

이게 있어야 **영상 조립**과 **프레임 뜯기**가 됩니다.
없어도 대본까지는 다 됩니다.

시작 버튼 옆 검색창에 `powershell` 치고 엔터. 그다음 이 한 줄:

```
winget install Gyan.FFmpeg
```

다 되면 **파워셸 창을 닫았다가 다시 여세요.** 안 그러면 인식이 안 됩니다.

확인하려면:

```
ffmpeg -version
```

> `winget` 이 없다는 말이 나오면 윈도우가 좀 오래된 겁니다.
> [ffmpeg.org](https://www.gyan.dev/ffmpeg/builds/) 에서 직접 받으셔야 합니다.
> 그건 좀 번거로우니 그때 저한테 말씀해주세요.

---

## 4단계 — 켜기

바탕화면 **결** 폴더를 열고 **`start.bat`** 를 두 번 누릅니다.

검은 창이 하나 뜹니다. **그 창은 닫지 마세요.** 앱이 그 안에서 돌고 있습니다.

처음 켤 때 클로드 API 키를 한 번 물어봅니다.
[console.anthropic.com](https://console.anthropic.com) 에서 만들어서 붙여넣으시면 됩니다.
한 번만 넣으면 다음부터는 안 물어봅니다.

잠시 뒤 브라우저가 스스로 열립니다.

**끄실 때는 검은 창을 닫으시면 됩니다.**

---

## 막힐 만한 데

| 이런 일이 생기면 | 이렇게 하세요 |
| --- | --- |
| 검은 창이 순식간에 사라진다 | 이제는 안 사라집니다. 멈추면 이유를 보여주고 기다립니다 |
| "파이썬이 안 깔려 있습니다" | 1단계에서 `Add Python to PATH` 체크를 빠뜨리신 겁니다. 다시 깔아주세요 |
| 윈도우 디펜더가 막는다 | **추가 정보 → 실행** 을 누르시면 됩니다 |
| 브라우저가 안 열린다 | 주소창에 `http://127.0.0.1:8000` 을 직접 치세요 |
| 한글이 깨져 보인다 | 검은 창 제목줄 오른쪽 클릭 → 속성 → 글꼴을 **굴림체** 로 바꾸세요 |
| ffmpeg 가 없다고 나온다 | 3단계 하신 뒤 **컴퓨터를 한 번 껐다 켜보세요** |

---

## 클로드랑 이어서 작업하실 거면

파워셸에서 결 폴더로 간 다음:

```
cd %USERPROFILE%\Desktop\결
claude
```

열자마자 지금 어디까지 했는지 뜹니다. 그리고 이렇게 말씀하시면 됩니다.

> 결 앱 이어서 하자. ffmpeg 조립부터 시험해보자.

---

## 내일 사무실에서 첫 할 일

1. 위 1~4단계로 앱을 켭니다
2. **영상 만들기** 탭에서 작업 폴더 경로를 확인합니다
3. 그 안 `screen` 폴더에 아무 그림이나 `01.png` `02.png` … 넣습니다
4. **조립해서 mp4로 뽑기** 를 눌러봅니다

이게 진짜로 되는지가 제일 궁금한 부분입니다. 아직 아무도 못 봤습니다.
